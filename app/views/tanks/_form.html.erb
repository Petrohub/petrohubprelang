<%= simple_form_for(@tank) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :gasstation %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
